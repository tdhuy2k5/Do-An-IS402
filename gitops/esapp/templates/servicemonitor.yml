{{- if .Values.metrics.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "esapp.fullname" . }}-monitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      {{- include "esapp.selectorLabels" . | nindent 6 }}
  namespaceSelector:
    matchNames:
      - {{ .Release.Namespace }}
  endpoints:
    - port: "http"
      path: /metrics
      interval: 15s
{{- end }}