apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "esapp.fullname" . }}-config
  labels:
    {{- include "esapp.labels" . | nindent 4 }}
data:
  base.env: |-
{{ .Files.Get "config.env" | indent 4 }}