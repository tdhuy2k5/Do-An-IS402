apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "esappfe.fullname" . }}-test-connection"
  labels:
    {{- include "esappfe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "esappfe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
