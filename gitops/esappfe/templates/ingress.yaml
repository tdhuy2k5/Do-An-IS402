

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .esappfe.fullname }}-ingress

spec:
  ingressClassName: haproxy

  rules:
  - host: fe.20.123.45.67.nip.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              number: 80